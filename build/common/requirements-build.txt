# Build dependencies for Friesen Enrollment Converter
# Install with: pip install -r build/common/requirements-build.txt

# PyInstaller for creating executables and app bundles
pyinstaller>=5.0.0

# Additional dependencies that might be needed for the build
# (These are already in requirements.txt but listed here for completeness)
customtkinter>=5.2.0
reportlab>=4.0.0 
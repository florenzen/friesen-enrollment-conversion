# Build dependencies for Windows executable creation
pyinstaller>=6.0.0
setuptools>=65.0.0

# Runtime dependencies (duplicated here for build environment)
customtkinter>=5.2.0

# Note: UPX compression is optional and handled automatically by PyInstaller
# If you want UPX compression, install UPX manually from https://upx.github.io/ 